/* Nos proporciona información sobre si las cookies están activadas, lenguaje que tiene el usuario en su navegador 📌 */

console.log('Cookies activadas', navigator.cookieEnabled);

//Accedemos al userAgent

console.log(navigator.userAgent);

// Detectar el lenguaje del navegador

console.log(navigator.language);

//Detectar si el navegador web esta conectado a internet.

console.log(navigator.onLine);
